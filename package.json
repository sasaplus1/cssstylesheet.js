{
  "name": "cssstylesheet",
  "description": "cross browser CSSStyleSheet",
  "version": "0.2.0",
  "author": "sasa+1 <sasaplus1@gmail.com>",
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "karma": "^0.13.22",
    "karma-chrome-launcher": "^0.2.3",
    "karma-espower-preprocessor": "0.0.4",
    "karma-firefox-launcher": "^0.1.7",
    "karma-mocha": "^0.2.2",
    "karma-safari-launcher": "^0.1.1",
    "parallelshell": "^2.0.0",
    "power-assert": "^1.3.1",
    "rimraf": "^2.5.2",
    "webpack": "^1.12.14"
  },
  "license": "MIT",
  "main": "./index.js",
  "readmeFilename": "README.md",
  "repository": {
    "type": "git",
    "url": "git://github.com/sasaplus1/cssstylesheet.js.git"
  },
  "scripts": {
    "build": "npm run clean && npm run compile && npm run minify",
    "clean": "npm run rimraf -- ./build",
    "compile": "npm run webpack -- --output-filename ./build/cssstylesheet.js",
    "develop": "parallelshell 'npm run compile -- -w' 'npm run minify -- -w'",
    "fixpack": "npx fixpack",
    "karma": "karma start --colors",
    "minify": "npm run webpack -- --optimize-minimize --output-filename ./build/cssstylesheet.min.js",
    "prepublish": "npm run build",
    "rimraf": "rimraf",
    "test": "npm run karma -- --single-run --browsers Firefox,Chrome,Safari",
    "webpack": "webpack --colors --display-error-details --progress"
  }
}
